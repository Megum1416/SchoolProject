@model IEnumerable<SchoolProject_DB.Models.RePost>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Post)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.PostID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RePostID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RePostID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RePostID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
